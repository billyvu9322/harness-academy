# Only the PASSWORD is a secret — keep it here in .env (gitignored).
# BASE_URL and USERNAME are non-secret defaults in tests/test-config.ts;
# set them here only to override per environment.
E2E_PASSWORD=

# Optional overrides (defaults live in tests/test-config.ts):
# E2E_BASE_URL=https://officeplace-ui-test.aod-team.com
# E2E_USERNAME=admin@aod.vn
